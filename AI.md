**Delegation** — What did you hand to AI, and what did you keep for yourself?

> I used AI to confirm the minimal boilerplate for a Flask/Express server and to sanity-check my curl syntax. I did not delegate the actual decision of what the two endpoints should return, or the git/GitHub workflow — I ran and understood every command myself.

**Description** — How did you prompt/instruct, and how specific were you?

> I gave the AI the exact assignment text so it could tailor the explanation to "two JSON endpoints, minimal code" rather than a generic tutorial, and I asked for both Flask and Express so I could compare before choosing.

**Discernment** — How did you judge/verify what the AI gave you?

> I checked that the code was genuinely minimal (no unnecessary middleware, no unused imports) and that it matched the "~20 lines" hint in the assignment. I ran it locally myself rather than assuming it would work.

**Diligence** — What did you personally verify/test rather than take on faith?

> I actually ran the server, hit both endpoints via curl and browser, and confirmed valid JSON came back in each case before pushing to GitHub. I also read through the GitHub push steps rather than blindly copy-pasting.